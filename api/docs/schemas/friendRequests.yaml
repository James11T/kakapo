components:
  friendRequest:
    type: object
    properties:
      user:
        $ref: "./users.yaml#/components/user"
      sentAt:
        $ref: "./generic.yaml#/components/date"

  sendFriendRequestSuccessResponse:
    type: object
    title: Friend request sent
    properties:
      id:
        $ref: "./generic.yaml#/components/uuid"

  existingFriendRequestResponse:
    type: object
    title: Already friends
    properties:
      error:
        type: string
        const: EXISTING_FRIEND_REQUEST
      message:
        type: string
        const: You already have a pending friend request with this user
